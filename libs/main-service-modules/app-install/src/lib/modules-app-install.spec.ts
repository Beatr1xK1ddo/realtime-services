import { modulesAppInstall } from './modules-app-install';

describe('modulesAppInstall', () => {
    it('should work', () => {
        expect(modulesAppInstall()).toEqual('modules-app-install');
    });
});
