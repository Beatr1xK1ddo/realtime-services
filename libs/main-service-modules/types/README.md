# main-service-modules-types

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test main-service-modules-types` to execute the unit tests via [Jest](https://jestjs.io).
