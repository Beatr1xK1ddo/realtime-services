import { mainServiceModulesNextomeet } from './main-service-modules-nextomeet';

describe('mainServiceModulesNextomeet', () => {
    it('should work', () => {
        expect(mainServiceModulesNextomeet()).toEqual(
            'main-service-modules-nextomeet'
        );
    });
});
